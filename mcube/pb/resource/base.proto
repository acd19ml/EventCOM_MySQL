syntax = "proto3";

package infraboard.mcube.resource;
option go_package = "github.com/acd19ml/EventCOM_MySQL/mcube/pb/resource";

enum VisiableMode {
    // 默认Namespace可见
    NAMESPACE = 0;
    // 全局可见
    GLOBAL = 1;
}